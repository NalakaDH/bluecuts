{
  "name": "backend",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "build": "echo \"No build step for backend\"",
    "wipe-data": "node scripts/wipe-all-data.js",
    "cloud-sync": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/cloud-sync.ps1",
    "cloud-sync:task-register": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/manage-cloud-sync-task.ps1 -Action Register -Schedule Both",
    "cloud-sync:task-hourly": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/manage-cloud-sync-task.ps1 -Action Register -Schedule Hourly",
    "cloud-sync:task-daily": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/manage-cloud-sync-task.ps1 -Action Register -Schedule Daily",
    "cloud-sync:task-unregister": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/manage-cloud-sync-task.ps1 -Action Unregister",
    "cloud-sync:task-list": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/manage-cloud-sync-task.ps1 -Action List"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Backend API for Blue Cuts Gems POS (Express + SQLite)",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.1",
    "express": "^5.2.1",
    "firebase-admin": "^13.7.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^1.4.5-lts.1",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
