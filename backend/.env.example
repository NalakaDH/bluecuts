## Local POS backend (Express + SQLite)
PORT=4000
BUSINESS_TZ=Asia/Bangkok

## Hybrid cloud sync (SQLite -> Firestore snapshots)
BLUECUTS_SHOP_ID=bluecuts
BLUECUTS_CLOUD_SYNC_SECRET=07135903350BCG

## Firebase Admin credentials (required for cloud sync — pick ONE)
## On a client PC: use a path that exists ON THAT PC, or use AppData (see cloud-sync.env.example).
# FIREBASE_SERVICE_ACCOUNT_PATH=C:\BlueCuts\firebase-service-account.json
# Or place the key here (auto-detected): %AppData%\Blue Cuts\firebase-service-account.json
# GOOGLE_APPLICATION_CREDENTIALS=C:\bluecuts\keys\firebase-service-account.json
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account", ...}

