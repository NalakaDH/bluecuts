/** sessionStorage key: invoice id to open on Invoice Checkout page (from Payments Pay). */
export const INVOICE_CHECKOUT_INVOICE_ID_KEY = 'app.invoiceCheckout.invoiceId';

/**
 * sessionStorage key: after converting a memo to an invoice, Selling reads this and opens
 * the “Invoice created” modal for that invoice id.
 */
export const SELLING_FROM_MEMO_CONVERT_INVOICE_KEY = 'app.selling.fromMemoConvert';
